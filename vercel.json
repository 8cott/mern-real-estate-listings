{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/listings", "dest": "/listings" },
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}
